services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nutrify-backend
    ports:
      - "${BACKEND_PORT}:3001"
    env_file:
      - backend/.env
    restart: unless-stopped
