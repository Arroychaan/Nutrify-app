# Environment
NODE_ENV=development
PORT=3000

# Database
# Option A: Supabase (RECOMMENDED - FREE & EASY)
# https://supabase.com → Settings → Database → Connection pooling (pgBouncer)
# Gunakan host aws-0-<region>.pooler.supabase.com dan port 5432
# Username harus <user>.<project_ref>, contoh: postgres.[PROJECT-REF]
# Password dengan karakter spesial harus di-URL encode (contoh: @ → %40)
# Tambahkan ?pgbouncer=true&connection_limit=1&sslmode=require
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres?pgbouncer=true&sslmode=require

# Kalau butuh prisma migrate dev, pakai direct URL (host db.<project>.supabase.co) yang mungkin hanya IPv6
# DATABASE_SHADOW_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require

# Option B: Docker Local
# DATABASE_URL=postgresql://postgres:password@localhost:5432/nutrify_dev
# DATABASE_SHADOW_URL=postgresql://postgres:password@localhost:5432/nutrify_shadow

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRE_TIME=24h
REFRESH_TOKEN_SECRET=your_super_secret_refresh_key
REFRESH_TOKEN_EXPIRE_TIME=7d

# LLM (Gemini AI)
# Get free API key from: https://aistudio.google.com/app/apikey
# If you have multiple keys for automatic rotation (comma-separated):
# GEMINI_API_KEYS=key1,key2,key3
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://nutrify.com

# External Services
AWS_S3_BUCKET=nutrify-dev
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Feature Flags
ENABLE_GAMIFICATION=true
ENABLE_OFFLINE_MODE=false
ENABLE_BIOMARKER_TRACKING=true

# Push Notifications (Web Push / VAPID)
# Generate keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_SUBJECT=mailto:your-email@example.com

# Logging
LOG_LEVEL=debug
LOG_FORMAT=json
